Upgrade Project Backend API
This project is a backend API built with Node.js, Express, TypeScript, and Firebase Firestore.

How to run:
npm install
npm run dev

API Endpoints:
GET /api/v1/departments
GET /api/v1/departments/:id
POST /api/v1/departments
PUT /api/v1/departments/:id
DELETE /api/v1/departments/:id
GET /api/v1/departments/:id/employee-count

Going forward, I want to keep improving the project. 
My next steps are:
- Add a few more tests so more parts of the app are covered.
- Clean up the code and make the API errors easier to understand.
- Add basic validation so only proper data gets saved.
- Improve the folder structure as the project grows.
- Add small features like filtering or sorting in the GET requests.

